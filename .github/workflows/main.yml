name: Flutter Direct APK Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.22.0'
        channel: 'stable'

    - name: Create Standard Flutter Project Structure
      run: |
        # 倉庫の直下にあるmain.dartを退避
        mkdir -p temp_src
        cp main.dart temp_src/
        
        # サーバー上で標準のFlutterプロジェクト構造を一瞬で自動生成
        flutter create --platforms=android . --force
        
        # 自動生成された初期lib/main.dartを、あなたが作った完璧なmain.dartで上書き置換
        cp temp_src/main.dart lib/main.dart
        rm -rf temp_src

    - name: Install Dependencies
      run: flutter pub get

    - name: Build Android APK
      run: flutter build apk --release --split-per-abi

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: guardian-core-apk
        path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
